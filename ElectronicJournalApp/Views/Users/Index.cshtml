@model IEnumerable<ElectronicJournalsApi.Models.User>

@{
    ViewData["Title"] = "Список пользователей";
}
@{
    Layout = "_Layout";
}
<head>
    <title>@ViewData["Title"]</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/index.css" asp-append-version="true" />
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <style>
        
    </style>
</head>

<body>
    <div class="container mx-auto p-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h2">Список пользователей</h1>
            <button onclick="window.location.href='/Users/Create'" class="btn btn-custom text-white px-4 py-2 rounded">
                <i class="fas fa-user-plus"></i> Добавить пользователя
            </button>
        </div>
        @* <div class="d-flex flex-column flex-lg-row"> *@
            @* <div class="bg-white p-4 rounded shadow-md mb-4 mb-lg-0 me-lg-4" style="flex: 0 0 25%;">
                <h5 class="h5 mb-4">Фильтры</h5>
                <div class="mb-2">
                    <span class="text-dropdown" onclick="toggleCollapse('filterRole')">
                        Роль <i id="filterRoleIcon" class="fas fa-chevron-down"></i>
                    </span>
                    <div id="filterRole" class="collapse mt-2">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" value="" id="roleAdmin">
                            <label class="form-check-label" for="roleAdmin">Администратор</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" value="" id="roleUser">
                            <label class="form-check-label" for="roleUser">Руководитель</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" value="" id="roleUser">
                            <label class="form-check-label" for="roleUser">Учитель</label>
                        </div>
                    </div>
                </div>
                <div>
                    <span class="text-dropdown" onclick="toggleCollapse('sortOptions')">
                        Сортировка <i id="sortOptionsIcon" class="fas fa-chevron-down"></i>
                    </span>
                    <div id="sortOptions" class="collapse mt-2">
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="sortOptions" id="sortName" value="name">
                            <label class="form-check-label" for="sortName">По фамилии</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="sortOptions" id="sortName" value="name">
                            <label class="form-check-label" for="sortName">По логину</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="sortOptions" id="sortRole" value="role">
                            <label class="form-check-label" for="sortRole">По роли</label>
                        </div>
                    </div>
                </div>
            </div> *@
            @* <div class="bg-white p-4 rounded shadow-md" style="flex: 0 0 75%;"> *@
            <div class="bg-white p-4 rounded shadow-md">
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <thead class="thead-light">
                            <tr>
                                <th class="text-center">@Html.DisplayName("ФИО")</th>
                                <th class="text-center">@Html.DisplayNameFor(model => model.First().Login)</th>
                                <th class="text-center">@Html.DisplayNameFor(model => model.First().Phone)</th>
                                <th class="text-center">@Html.DisplayNameFor(model => model.First().BirthDate)</th>
                                <th class="text-center">@Html.DisplayNameFor(model => model.First().Role)</th>
                                <th class="text-center">Действия</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model.Any())
                            {
                                foreach (var item in Model)
                                {
                                    <tr id="user-row-@item.IdUsers">
                                        <td class="text-center">@($"{item.Surname} {item.Name} {item.Patronymic}")</td>
                                        <td class="text-center">@Html.DisplayFor(modelItem => item.Login)</td>
                                        <td class="text-center">@Html.DisplayFor(modelItem => item.Phone)</td>
                                        <td class="text-center">@Html.DisplayFor(modelItem => item.BirthDate)</td>
                                        <td class="text-center">@Html.DisplayFor(modelItem => item.Role)</td>
                                        <td class="text-center">
                                            <button title="Информация" class="btn btn-info btn-sm mx-1 btn-icon">
                                                <i class="fas fa-info-circle"></i>
                                            </button> 
                                            <button title="Редактировать" class="btn btn-edit btn-sm mx-1 btn-icon">
                                                <i class="fas fa-edit"></i>
                                            </button>
                                            <button title="Удалить" class="btn btn-danger btn-sm mx-1 btn-icon" onclick="deleteUser(@item.IdUsers); return confirm('Вы уверены, что хотите удалить этого пользователя?');">
                                                <i class="fas fa-trash-alt"></i>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="6" class="text-center">Нет пользователей для отображения</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        @* </div> *@
    </div>

    <script src="~/js/userIndex.js"></script>
</body>

